# Puerto del servidor (Cloud Run usa el variable PORT automático)
PORT=3000

# Configuración de Google Cloud
# Obtén tu PROJECT_ID desde: https://console.cloud.google.com
GCP_PROJECT_ID=tu-proyecto-gcp

# OAuth2 Access Token
# Genera con: gcloud auth print-access-token
# O desde Firebase Auth con Google Sign-In
ACCESS_TOKEN=tu-token-oauth2

# Modelo por defecto (opcional)
# Opciones: gemini-3-pro-high | claude-sonnet-4-5 | claude-sonnet-4-5-thinking
DEFAULT_MODEL=gemini-3-pro-high

# Configuración de generación (opcional)
MAX_TOKENS=1000
TEMPERATURE=0.7

# Entorno
NODE_ENV=development
